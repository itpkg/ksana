up = ["CREATE TABLE users(id SERIAL NOT NULL PRIMARY KEY, username VARCHAR(255) NOT NULL, email VARCHAR(255) NOT NULL, status VARCHAR(8) NOT NULL DEFAULT 'submit', password BYTEA, provider_id VARCHAR(25) NOT NULL, provider_type VARCHAR(16) NOT NULL DEFAULT 'email', profile BYTEA, updated TIMESTAMP NOT NULL, created TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP)", "CREATE UNIQUE INDEX idx_users_email ON users (email)", "CREATE INDEX idx_users_status ON users (status)", "CREATE INDEX idx_users_username ON users (username)", "CREATE TABLE logs(id SERIAL NOT NULL PRIMARY KEY, user INT NOT NULL, message VARCHAR(255) NOT NULL, type VARCHAR(8) NOT NULL DEFAULT 'INFO', created TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP)", "CREATE INDEX idx_logs_type ON logs (type)"]
down = ["DROP TABLE logs", "DROP TABLE users"]
